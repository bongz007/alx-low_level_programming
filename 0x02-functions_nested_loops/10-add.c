#include "main.h"
#include <stdio.h>

/**
 * add - prints two numbers added together
 * @i: print int i
 * @k: print int k
 * Return: always 0 (success)
 */

int add(int i, int k)
{
	return (i + k);
}

